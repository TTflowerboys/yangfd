image: felixonmars/archlinux

stages:
  - test
  - deploy

before_script:
  - "git config submodule.'source/server/libfelix'.url 'https://git.bbtechgroup.com/felixonmars/libfelix.git'"
  - git submodule update --init

server_test:
  stage: test
  script:
    - cd source/server
    - python2 wsgi.py --test
  tags:
    - docker

server_deploy:
  stage: deploy
  script:
    - "echo 'fixme: server_deploy stub'"
  tags:
    - docker
